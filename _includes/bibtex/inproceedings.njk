<i>{{ publication.author }}</i>. <b>{{ publication.title }}</b>,
{% if publication.month %}
  ({{ publication.month }} {{ publication.year }}),
{% else %}
  ({{ publication.year }}),
{% endif %}
{{ publication.booktitle }} - {{ publication.address }}
{% if publication.url %}
 - (<a href={{ publication.url }}>url</a>)
{% endif %}